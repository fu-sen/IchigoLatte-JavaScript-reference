env
1.0.0b～
-----------------------------------------------------------
[ 機能 ]
環境変数の設定・取得を行います。

[ 書式 ]
env([<値>])

[ 例 ]
env(10);
env(a);
a=env();

[ 解説 ]
<値> を入れている場合は環境変数へ <値> を設定します。
<値> を省略した場合は設定していた値を取り出し、返します。

